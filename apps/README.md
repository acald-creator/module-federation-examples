# Apps Directory

This directory contains different Module Federation example implementations.

## Structure

- `nextjs-cra/` - Next.js host with Create React App remotes example
- Future examples will be added here with descriptive names

Each example should be self-contained with its own README and documentation.